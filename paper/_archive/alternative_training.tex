\section{Alternative Training Methods Explored}\label{sec:appendix-i}

We explored 2 more pipelines for persona training: DPO-only (the first stage of Open Character Training) and SFT-only (the second stage). For both, we use smaller edited answer samples. For SFT-only, we are able to achieve visible behavioural steering on as little as 500 edited samples.

Below, we include TRAIT eval for the SFT-only adapter to show that behaviour is still scalable and measurable.

% DUMMY --- replace with publication-quality figure
\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/tmp/image49.png}
\caption{TRAIT benchmark scores for models produced by applying an SFT-only trained LoRA at different scales.}
% TODO: verify caption matches final figure
\label{fig:I-sft-only-trait}
\end{figure}

We picked the Open Character Training pipeline as it provided the most stable and robust behaviour out of all explored options.
\FloatBarrier